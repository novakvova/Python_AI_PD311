## Використання gTTS - Python 3.13.7
```
py --version
py -m venv .venv
.venv\Scripts\activate.bat

python.exe -m pip install --upgrade pip

python main.py

pip install yt-dlp

pip install ffmpeg-python

ffmpeg -version

pip install spotdl

choco install ffmpeg

python spotify_downloader.py

```